version: 0.0

os: linux

files:
  - source: application/
    destination: /usr/share/nginx/html/

file_exists_behavior: OVERWRITE

permissions:
  - object: /usr/share/nginx/html/
    pattern: "**"
    owner: nginx
    group: nginx
    mode: 755
